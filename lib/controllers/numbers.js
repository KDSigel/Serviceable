const Router = require('express');

module.exports = Router()
    .post('/', (res, req, next) => {
        // call nasa service
    })
    .get('/', (res, req, next) => {
        // make a get all
    })